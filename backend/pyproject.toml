[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests",
    "jinja2",
    "orjson",
    "pandas",
    "openpyxl",
    "reportlab"
]

[dependency-groups]
dev = [
    "pytest",
    "black",
    "ruff"
]
